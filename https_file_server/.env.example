# No secrets required for the OTA stack.
# Tokens are managed as files in /opt/signalfi/tokens/ via SFTP.
# Use generate-manifest.js to create and upload tokens automatically.
#
# SFTP credentials (symphony user) are set in sftpgo/init-data.json
# The symphony user password is: Si9057274427
